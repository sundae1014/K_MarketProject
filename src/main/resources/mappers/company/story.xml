<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.kmarket.mapper.StoryMapper">
    <select id="selectCardData" parameterType="kr.co.kmarket.dto.StoryDTO">
        select story_no, cate, subtitle, img_path, title from story
    </select>

    <select id="selectById" parameterType="kr.co.kmarket.dto.StoryDTO">
        select story_no, title, author, img_path, content, TO_CHAR(reg_date, 'YYYY-MM-DD') AS reg_date from story
        where story_no = #{story_no}
    </select>

    <select id="selectRecent" parameterType="kr.co.kmarket.dto.StoryDTO">
        select story_no, title, TO_CHAR(reg_date, 'YYYY-MM-DD') AS reg_date from story
        order by reg_date
        fetch first 5 rows only
    </select>
</mapper>